<Window xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        Title="MainWindow" 
        MinHeight="600" 
        MinWidth="800" 
        Height="600"
        Width="800"
        WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="60"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="40"/>
        </Grid.RowDefinitions>

        <Rectangle Style="{DynamicResource LeftBackground}" Grid.Column="0" Grid.RowSpan="4" />
        <Rectangle Style="{DynamicResource RightBackground}" Grid.Column="1" Grid.RowSpan="4" Grid.ColumnSpan="2"/>


        <TextBlock Grid.Row="0"
                   Grid.Column="0"
                   FontFamily="Steiner" 
                   FontSize="58" 
                   TextAlignment="Right"
                   Style="{DynamicResource LeftTitle}">
            Mech
        </TextBlock>
        <TextBlock Grid.Row="0" 
                   Grid.Column="1" 
                   FontFamily="Steiner" 
                   FontSize="58" 
                   Grid.ColumnSpan="2"
                   Style="{DynamicResource RightTitle}">
            Warrior 2
        </TextBlock>
        <ContentControl Grid.Row="1" 
                        Grid.Column="0" 
                        VerticalAlignment="Top"
                        ContentTemplate="{DynamicResource SubtitleTextBlock}"/>

        <Grid Grid.Row="1" Grid.Column="1">

            <Grid x:Name="AgreementStep"
                  Visibility="Visible">
                <Grid.RowDefinitions>
                    <RowDefinition Height="30"/>
                    <RowDefinition Height="*" />
                    <RowDefinition Height="50"/>
                    <RowDefinition Height="50" />
                </Grid.RowDefinitions>
                <TextBlock FontSize="20" 
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center">
                    Welcome to the MechWarrior 2 Installer
                </TextBlock>
                <TextBox x:Name="AgreementText"   
                         Grid.Row="1"
                         IsReadOnly="True"
                         VerticalScrollBarVisibility="Visible"
                         TextWrapping="WrapWithOverflow">
                </TextBox>
                <CheckBox x:Name="AcceptCheck"
                          Grid.Row="2"
                          VerticalAlignment="Center"
                          HorizontalAlignment="Center">
                    I have read the above, I accept all risks, and I am ready to proceed.
                </CheckBox>
                <Grid Grid.Row="3">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="100"/>
                        <ColumnDefinition />
                        <ColumnDefinition Width="100"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock VerticalAlignment="Center" HorizontalAlignment="Center">
                        Install Path
                    </TextBlock>
                    <TextBox Grid.Column="1" Margin="5,10" x:Name="InstallPath" Padding="5,5,5,4" Text="C:\Games"/>
                    <Button Grid.Column="2" 
                            x:Name="BrowseButton"
                            Margin="5,10">
                        Browse
                    </Button>
                </Grid>
            </Grid>

            <Grid x:Name="InstallStep"
                  Visibility="Hidden">
                <Grid.RowDefinitions>
                    <RowDefinition Height="40" />
                    <RowDefinition />
                </Grid.RowDefinitions>
                <TextBlock x:Name="CurrentStatus" 
                           FontSize="14"/>
                <TextBox x:Name="ActionLog"
                         Grid.Row="1"
                         IsReadOnly="True"
                         VerticalScrollBarVisibility="Visible"/>

            </Grid>
        </Grid>
        <Grid Grid.Row="3" 
              Grid.Column="2"
              Style="{DynamicResource BottomBackground}">
            <Grid x:Name="ButtonGrid">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="100" />
                    <ColumnDefinition />
                    <ColumnDefinition Width="100" />
                </Grid.ColumnDefinitions>
                <Button x:Name="BackButton"
                        Grid.Column="0"
                        Margin="5">
                    Close
                </Button>
                <Button x:Name="NextButton"
                        Grid.Column="2"
                        Margin="5"
                        IsEnabled="{Binding IsChecked, ElementName=AcceptCheck}">
                    Next
                </Button>
            </Grid>
            <Grid x:Name="ProgressGrid"
                  Visibility="Hidden">
                <ProgressBar Grid.Row="1" 
                             Margin="5"
                             x:Name="ProgressBar" />

            </Grid>
        </Grid>
    </Grid>
</Window>